<div class="container">
  <div class="col-md-12">
    <h1 class="display-3 text-center">Vælg en kategori!</h1>
    <br>
    <div class="card-group">
      <% @category_names.each_key do |key| %>
          <%= render 'index_card', category: key %>
      <% end %>
    </div>
  </div>

  <h1 class="lead text-center">Aktivitet</h1>
  <div class="offset-md-2 col-md-8 bg-faded" style="box-shadow: 0px 0px 5px #b4b4b4">
      <div style="padding-top: 0px; padding-bottom: 0px;">
        <ul class="timeline">
          <% for illness in @new_illnesses %>
            <%= render 'index_timeline_element', illness: illness, counter: @counter %>
            <% @counter += 1 %>
         <% end %>
        </ul>
      </div>
  </div>
<!-- end of row -->
</div>

